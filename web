import React, { useState } from 'react';
import { 
  BookOpen, 
  Search, 
  Award, 
  X, 
  ChevronRight, 
  Info, 
  Target,
  GraduationCap,
  Sparkles,
  ArrowRight
} from 'lucide-react';

// --- ROZŠÍŘENÁ DATA Z TVÉHO LISTU 1 ---
const SEMINARS_DATA = [
  {
    id: "B2F",
    title: "Přípravný seminář k mezinárodní zkoušce z AJ - úroveň B2 (VÝBĚROVÝ)",
    shortName: "B2F",
    targetSchools: "Všechny školy vyžadující mezinárodní jazykový certifikát, zahraniční univerzity.",
    focus: "Cizí jazyky",
    important: "Vstupní test se skládá na konci 2. ročníku. Pouze pro studenty 3. ročníku.",
    annotation: "Chcete proměnit své znalosti angličtiny v oficiální certifikát, který vám otevře dveře na univerzity a k lepším pracovním nabídkám? Tento výběrový seminář je zaměřen na efektivní a intenzivní přípravu k mezinárodní zkoušce na úrovni B2. Podmínkou přijetí je úspěšné složení vstupního testu, který zaručuje, že tempo výuky bude odpovídat vašim pokročilým schopnostem. Seminář vyžaduje vysokou míru samostatnosti.",
    expectations: "Komplexní nácvik: Cílené procvičování všech testovaných oblastí – poslechu, psaní, mluveného projevu a Use of English. Strategie úspěchu: Osvojení technik pro řešení úloh v časovém limitu. Práce s autentickými testy a simulace zkoušky nanečisto.",
    tags: ["3. ročník", "certifikát"]
  },
  {
    id: "LS3",
    title: "Literární seminář pro 3. ročník",
    shortName: "LS3",
    targetSchools: "Humanitní fakulty, pedagogika, žurnalistika, filozofie, umělecké obory.",
    focus: "Český jazyk",
    important: "Vhodný základ pro ústní část maturitní zkoušky.",
    annotation: "Seminář je určen studentům, kteří si chtějí vytvořit pevný základ pro úspěšné zvládnutí ústní zkoušky z českého jazyka. Zaměříme se na četbu a interpretaci vybraných literárních děl, diskusi o tématech, postavách a literárních směrech. Naučíte se číst s porozuměním a nadhledem.",
    expectations: "Diskuse o tématech a postavách. Vyzkoušíte si různé způsoby práce s textem. Formulace vlastních názorů na přečtené dílo. Procvičení dovedností potřebných k ústní maturitě.",
    tags: ["3. ročník"]
  },
  {
    id: "LS4",
    title: "Literární seminář pro 4. ročník",
    shortName: "LS4",
    targetSchools: "Humanitní obory, práva, žurnalistika, politologie.",
    focus: "Český jazyk",
    important: "Navazuje na 3. ročník. Nováčci si musí učivo z 3. ročníku dostudovat sami.",
    annotation: "V závěrečném ročníku se seminář stává intenzivním tréninkovým prostorem pro maturitní zkoušku. Zaměříme se na praktickou analýzu uměleckého textu přímo podle maturitních kritérií. Cílem je získat suverenitu v mluveném projevu a schopnost reagovat na otázky komise.",
    expectations: "Maturitní simulace: Pravidelný nácvik ústní části se zpětnou vazbou. Analýza na míru: Zaměření na konkrétní díla z maturitního kánonu. Rétorika a prezentace čtenářského seznamu.",
    tags: ["4. ročník"]
  },
  {
    id: "CJP",
    title: "Cvičení z programování",
    shortName: "CJP",
    targetSchools: "Matfyz, ČVUT, VUT, kybernetika a další technické obory.",
    focus: "Informatika",
    important: "Základní znalost algoritmizace je velkou výhodou.",
    annotation: "Seminář pro ty, kteří se chtějí věnovat vývoji softwaru nebo systémovému inženýrství. Prohloubení znalostí v programovacích jazycích a tvorba vlastních projektů.",
    expectations: "Práce na reálných projektech. Pochopení složitějších datových struktur. Týmový vývoj a debugování kódu.",
    tags: ["3. ročník", "4. ročník"]
  },
  {
    id: "ZMP",
    title: "Zeměpisný seminář",
    shortName: "ZMP",
    targetSchools: "Regionální rozvoj, cestovní ruch, mezinárodní vztahy, ekologie.",
    focus: "Zeměpis",
    important: "Vhodné pro maturanty i zájemce o cestovní ruch.",
    annotation: "Třetí ročník je zaměřen na cestovní ruch, který se prolíná všemi regionálními otázkami. Ve čtvrtém ročníku jde o cílenou přípravu k maturitě.",
    expectations: "Práce s mapou a statistickými daty. Regionální přehledy. Aktuální geopolitické souvislosti.",
    tags: ["3. ročník", "4. ročník"]
  }
];

export default function App() {
  const [searchTerm, setSearchTerm] = useState("");
  const [gradeFilter, setGradeFilter] = useState("Vše");
  const [selectedSeminar, setSelectedSeminar] = useState(null);

  const filtered = SEMINARS_DATA.filter(s => {
    const matchesSearch = s.title.toLowerCase().includes(searchTerm.toLowerCase()) || 
                         s.shortName.toLowerCase().includes(searchTerm.toLowerCase());
    const matchesGrade = gradeFilter === "Vše" || s.tags.includes(`${gradeFilter}. ročník`);
    return matchesSearch && matchesGrade;
  });

  return (
    <div className="min-h-screen bg-[#F8FAFC] text-slate-900 font-sans pb-12">
      {/* Navbar */}
      <nav className="bg-white/80 backdrop-blur-md border-b border-slate-200 sticky top-0 z-30">
        <div className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className="bg-indigo-600 p-2 rounded-xl text-white">
              <BookOpen size={22} />
            </div>
            <span className="text-xl font-black tracking-tight text-slate-800">
              KATALOG<span className="text-indigo-600">2025</span>
            </span>
          </div>
        </div>
      </nav>

      <main className="max-w-6xl mx-auto px-6 py-10">
        {/* Hero */}
        <div className="mb-12">
          <h1 className="text-4xl md:text-5xl font-black text-slate-900 mb-4 tracking-tight">
            Vyberte si svůj seminář
          </h1>
          <p className="text-slate-500 text-lg max-w-2xl leading-relaxed">
            Kompletní přehled volitelných předmětů pro 3. a 4. ročník. Klikněte na kartu pro detailní informace a anotaci.
          </p>
        </div>

        {/* Vyhledávání a Filtry */}
        <div className="flex flex-col md:flex-row gap-4 mb-10">
          <div className="relative flex-1 group">
            <Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 group-focus-within:text-indigo-500 transition-colors" size={20} />
            <input 
              type="text" 
              placeholder="Hledat seminář..."
              className="w-full pl-12 pr-4 py-4 bg-white border border-slate-200 rounded-2xl shadow-sm outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all"
              value={searchTerm}
              onChange={(e) => setSearchTerm(e.target.value)}
            />
          </div>
          
          <div className="flex p-1.5 bg-white border border-slate-200 rounded-2xl shadow-sm">
            {["Vše", "3", "4"].map(g => (
              <button
                key={g}
                onClick={() => setGradeFilter(g)}
                className={`px-6 py-2.5 rounded-xl font-bold transition-all text-sm ${
                  gradeFilter === g 
                  ? "bg-indigo-600 text-white shadow-lg shadow-indigo-100" 
                  : "text-slate-500 hover:bg-slate-50"
                }`}
              >
                {g === "Vše" ? "Všechny" : `${g}. ročník`}
              </button>
            ))}
          </div>
        </div>

        {/* Seznam karet */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {filtered.map(s => (
            <div 
              key={s.id} 
              onClick={() => setSelectedSeminar(s)}
              className="bg-white border border-slate-200 rounded-[2rem] p-7 shadow-sm hover:shadow-xl hover:-translate-y-1.5 transition-all cursor-pointer group relative overflow-hidden"
            >
              <div className="absolute top-0 right-0 p-6 opacity-5 group-hover:opacity-10 transition-opacity">
                <BookOpen size={80} />
              </div>

              <div className="flex flex-wrap gap-2 mb-4">
                {s.tags.map(tag => (
                  <span 
                    key={tag} 
                    className={`text-[10px] font-black px-3 py-1 rounded-full uppercase tracking-widest ${
                      tag === "certifikát" ? "bg-amber-100 text-amber-700" : "bg-slate-100 text-slate-600"
                    }`}
                  >
                    {tag}
                  </span>
                ))}
              </div>
              
              <div className="text-xs font-bold text-indigo-500 mb-1">{s.shortName}</div>
              <h3 className="text-xl font-bold text-slate-800 leading-tight mb-6">
                {s.title}
              </h3>

              <div className="mt-auto flex items-center gap-2 text-indigo-600 font-bold text-sm">
                <span>Více informací</span>
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </div>
            </div>
          ))}
        </div>

        {filtered.length === 0 && (
          <div className="text-center py-24 bg-white rounded-[3rem] border-2 border-dashed border-slate-200">
            <p className="text-slate-400 font-bold text-lg">Zadaným kritériím nic neodpovídá.</p>
          </div>
        )}
      </main>

      {/* MODÁLNÍ OKNO - DETAIL */}
      {selectedSeminar && (
        <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900/40 backdrop-blur-sm animate-in fade-in duration-300">
          <div className="bg-white w-full max-w-3xl max-h-[92vh] overflow-hidden rounded-[2.5rem] shadow-2xl flex flex-col">
            
            {/* Header modálu */}
            <div className="p-8 pb-4 flex justify-between items-start sticky top-0 bg-white z-10">
              <div className="pr-8">
                <div className="flex items-center gap-3 mb-3">
                  <span className="text-xs font-black text-indigo-600 bg-indigo-50 px-3 py-1 rounded-full uppercase tracking-widest border border-indigo-100">
                    {selectedSeminar.shortName}
                  </span>
                  <span className="text-xs font-bold text-slate-400 uppercase tracking-widest">
                    {selectedSeminar.focus}
                  </span>
                </div>
                <h2 className="text-3xl font-black text-slate-900 leading-[1.15]">
                  {selectedSeminar.title}
                </h2>
              </div>
              <button 
                onClick={() => setSelectedSeminar(null)}
                className="p-3 bg-slate-100 hover:bg-slate-200 rounded-2xl transition-all text-slate-500"
              >
                <X size={24} />
              </button>
            </div>

            {/* Obsah modálu */}
            <div className="px-8 pb-10 overflow-y-auto space-y-10 scroll-smooth">
              
              {/* Anotace */}
              <div className="space-y-4">
                <div className="flex items-center gap-2 text-indigo-600 font-black uppercase text-xs tracking-[0.2em]">
                  <Sparkles size={16} />
                  <span>Anotace semináře</span>
                </div>
                <div className="text-slate-600 leading-relaxed text-lg font-medium p-7 bg-slate-50 rounded-[2rem] border border-slate-100">
                  {selectedSeminar.annotation}
                </div>
              </div>

              {/* Co čekat */}
              <div className="space-y-4">
                <div className="flex items-center gap-2 text-slate-800 font-black uppercase text-xs tracking-[0.2em]">
                  <GraduationCap size={16} />
                  <span>Co vás v semináři čeká</span>
                </div>
                <div className="grid gap-4">
                  {selectedSeminar.expectations.split('. ').map((point, idx) => (
                    point && (
                      <div key={idx} className="flex gap-4 items-start p-5 bg-white border border-slate-100 rounded-2xl shadow-sm hover:border-indigo-200 transition-colors">
                        <div className="bg-indigo-50 p-2 rounded-lg text-indigo-600 shrink-0">
                          <ChevronRight size={16} />
                        </div>
                        <p className="text-slate-600 font-medium">{point.trim()}{point.trim().endsWith('.') ? '' : '.'}</p>
                      </div>
                    )
                  ))}
                </div>
              </div>

              {/* Infobox: Školy a Důležité info */}
              <div className="grid md:grid-cols-2 gap-6">
                <div className="p-7 bg-indigo-600 rounded-[2rem] text-white shadow-xl shadow-indigo-100">
                  <div className="flex items-center gap-2 font-black uppercase text-[10px] tracking-widest text-indigo-200 mb-3">
                    <Target size={18} />
                    <span>Kam dál?</span>
                  </div>
                  <p className="text-indigo-50 font-bold leading-relaxed">
                    {selectedSeminar.targetSchools}
                  </p>
                </div>

                <div className="p-7 bg-amber-50 rounded-[2rem] border border-amber-100">
                  <div className="flex items-center gap-2 font-black uppercase text-[10px] tracking-widest text-amber-600 mb-3">
                    <Info size={18} />
                    <span>Upozornění</span>
                  </div>
                  <p className="text-amber-900/80 font-bold leading-relaxed italic">
                    {selectedSeminar.important}
                  </p>
                </div>
              </div>
            </div>

            {/* Footer modálu */}
            <div className="p-8 pt-4 border-t border-slate-50 bg-white">
              <button 
                onClick={() => setSelectedSeminar(null)}
                className="w-full py-4.5 bg-slate-900 text-white rounded-2xl font-black text-sm tracking-widest hover:bg-indigo-600 transition-all transform active:scale-[0.98] shadow-lg"
              >
                ZAVŘÍT DETAIL
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
